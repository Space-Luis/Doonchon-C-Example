#include <stdio.h> // 헤더파일을 불러오는 코드 

int main(){ // main() 함수 선언 
            // C언어는 가장 처음에 실행하는 함수가 main() 함수입니다.
            // main() 함수가 없으면 컴파일이 되지 않습니다.
    printf("Hello World!\n"); // Hello World! 라는 문자열을 출력하는 코드

    return 0; //0이라는 값을 반환함으로써 해당 함수를 성공적으로 끝냈다는 것을 알려주기 위한 코드
}